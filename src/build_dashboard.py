"""
초기 정적 대시보드 빌드 스크립트.

이 프로젝트는 정적 HTML을 직접 생성하는 대신 api_server.py(FastAPI)가
런타임에 /api/dashboard-data를 통해 데이터를 제공하는 방식으로 진행합니다.
집계 로직은 aggregations.py에 공용 모듈로 분리되어 있어 이 스크립트가
필요해질 경우(예: 정적 스냅샷 생성) 그대로 재사용할 수 있습니다.

현재 단계에서는 별도 구현 없이 자리만 잡아둡니다.
"""
